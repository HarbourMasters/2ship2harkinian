/* soh/resource/type/SohResourceType.h shim. Keeps the include resolvable; the
 * SOH_ResourceType enum values used by the form engine surface at build for mapping. */
#ifndef NEI_SHIM_RES_SOHRESOURCETYPE_H
#define NEI_SHIM_RES_SOHRESOURCETYPE_H
#include "soh/_nei_compat_core.h"
#endif
