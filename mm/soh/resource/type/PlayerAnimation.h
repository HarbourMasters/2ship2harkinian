/* soh/resource/type/PlayerAnimation.h shim. SoH-specific resource type used by the
 * form-engine renderers; keeps the include resolvable. If 2ship lacks an exact
 * equivalent the type surfaces at build and gets mapped to 2ship's anim resource. */
#ifndef NEI_SHIM_RES_PLAYERANIMATION_H
#define NEI_SHIM_RES_PLAYERANIMATION_H
#include "soh/_nei_compat_core.h"
#endif
