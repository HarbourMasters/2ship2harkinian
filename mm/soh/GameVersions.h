/* soh/GameVersions.h shim. 2ship has its own version constants; this just keeps
 * the include resolvable. Map specific OOT_* version checks at the call site if
 * the build flags any as undefined. */
#ifndef NEI_SHIM_GAMEVERSIONS_H
#define NEI_SHIM_GAMEVERSIONS_H
#include "soh/_nei_compat_core.h"
#endif
